services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-replaykit}
      POSTGRES_USER: ${POSTGRES_USER:-replaykit}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-replaykit}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - replaykit-postgres-data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  replaykit-postgres-data:
